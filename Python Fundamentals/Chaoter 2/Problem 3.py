# 3. Check the type of variable assigned using input () function

a = input("Enter the value of a : ")  #if you are enter a number that is always str form 
print(type(a))
b = int(input("Enter a number : "))  # now yoy found a integer number
print(type(b))
